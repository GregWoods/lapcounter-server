; not sure this service is needed. Docker seems to start automatically, with container already running. I need to be sure of this though!

[Unit]
Description=Lapcounter Server Docker Compose Application
Requires=docker.service
After=docker.service

[Service]
WorkingDirectory=/path/to/your/docker-compose/directory
ExecStart=/usr/bin/docker-compose up
ExecStop=/usr/bin/docker-compose down
Restart=always

[Install]
WantedBy=multi-user.target