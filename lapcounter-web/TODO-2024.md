* Minimal changes to get this working with the Mosquitto broker running in docker
* Dockerise this web app
* Start from scratch using something which isn't Create React App
* Move  logic to a backend API
